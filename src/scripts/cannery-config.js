const CANNERY_CONFIG = {
    queryName: 'test',  // required
    queryParams: {greeting: 'yo'},  // optional (but must match query to run)
    // Uncomment below if you want output file name different than queryName
    // outputName: 'test'
}

module.exports = CANNERY_CONFIG
